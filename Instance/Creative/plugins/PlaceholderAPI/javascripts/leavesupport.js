var placeholder = "%hyacinthhello_leave%";
var leave = PlaceholderAPI.static.setPlaceholders(BukkitPlayer, placeholder);

function supportleave() {
    var bro = leave;
    return bro;
}
supportleave();